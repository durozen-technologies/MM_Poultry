name: Backend CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: root
          POSTGRES_DB: mmbroilers_test
        ports:
          - "5432:5432"
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
      POSTGRES_SERVER: localhost
      POSTGRES_PORT: 5432
      POSTGRES_DB: mmbroilers_test
      SECRET_KEY: test-secret-key-with-32-chars-minimum
    steps:
      - uses: actions/checkout@v4.4.0
      - uses: astral-sh/setup-uv@v5.4.2
        with:
          enable-cache: true
      - name: Install dependencies
        working-directory: backend
        run: uv sync
      - name: Create legacy test DB (MM_Poultry_test for backend/tests)
        run: |
          sudo apt-get update -qq && sudo apt-get install -y -qq postgresql-client
          PGPASSWORD=root createdb -h localhost -U postgres MM_Poultry_test || true
          PGPASSWORD=root createdb -h localhost -U postgres mmbroilers_test || true
      - name: Ruff
        working-directory: backend
        run: uv run ruff check app
      - name: Mypy
        working-directory: backend
        run: uv run mypy app/
      - name: Pytest
        working-directory: backend
        run: uv run pytest -q
